# 2026-04-21

## Top of Mind
- Dotfiles cleanup

## Today's Focus
- Housekeeping pass on the dotfiles repo

## Notes

### Session 1 Summary

**What was done:**
- Removed two stale tmux debug logs (`tmux-client-20563.log` from 2019, `tmux-client-45573.log` from 2017) from repo root

**Key things to remember:**
- Dotfiles cleanup is ongoing — only tackled tmux logs so far
- Inbox and daily note structure were created during this session

### Session 2 Summary — Obsidian Vault Integration

**What was decided:**
- Use symlinks (not copies) to get external files into the Obsidian vault
- Daily notes from multiple projects live under `vault/daily/<project>/` as symlinked folders
- Current structure: `vault/daily/dotfiles/` -> `/Users/crac/dotfiles/daily/`

**What was done:**
- Created symlink from dotfiles daily folder into the vault
- Restructured `vault/daily/` from a flat symlink to a real folder with per-project subfolders
- Added `vault-link` function — general purpose symlink into vault
- Added `vault-daily` function — links a project's `daily/` folder into `vault/daily/<project>`

**Key things to remember:**
- `vault-link <source> [name]` — symlinks anything into the vault root
- `vault-daily [folder] [name]` — from any project dir, run `vault-daily` to register its daily folder
- Vault path: `/Users/crac/development/vaults`
- Functions live in `~/dotfiles/functions`

**Next actions:**
- Continue dotfiles cleanup: dead aliases, unused scripts in `bin/`, stale config files
- Run `vault-daily` from other project directories as needed
